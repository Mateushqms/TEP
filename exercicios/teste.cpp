#include <bits/stdc++.h>

using namespace std;

/*Funções do vector
vector<int> xs(9, 123); Declara um vetor de 9 inteiros, todos com valor 123
Ex:
Declaração vector<int> xs {1,2,3,4,5};
Verificação de tamanho: xs.size();
Adicionar elemento no final: xs.emplace_back(6); xs.push_back(6); emplace_back é mais eficiente
Remover o último elemento: xs.pop_back();
xs.front(); // retorna o primeiro elemento
xs.back(); // retorna o último elemento
xs[i]; // retorna o i-ésimo elemento (0-indexado)
xs.clear(); // remove todos os elementos
std::iota(xs.begin(), xs.end(), 1); // Preenche o vetor com valores consecutivos, começando em 1

*/
